-- ============================================================
-- Миграция №5: чинит отсутствующую политику UPDATE для events
-- (без неё сохранение видимости по отделам молча не срабатывало)
-- Выполнить в Supabase: SQL Editor -> New query -> вставить -> Run
-- ============================================================

create policy "events_update_all" on events for update using (true) with check (true);
