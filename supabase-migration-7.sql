-- ============================================================
-- Миграция №7: чинит отсутствующую политику DELETE для app_users
-- (без неё удаление пользователей молча не срабатывало)
-- Выполнить в Supabase: SQL Editor -> New query -> вставить -> Run
-- ============================================================

create policy "users_delete_all" on app_users for delete using (true);
